spring:
  datasource:
    url: jdbc:h2:mem:jblyInMemory;MODE=MYSQL;NON_KEYWORDS=USER
    username: lee
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true
